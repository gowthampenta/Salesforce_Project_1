<apex:page controller="Array_Prac_1">
    <apex:form >
       <apex:pageBlock >
         <apex:pageBlockTable value="{!accarray}" var="acc">
                <apex:column value="{!acc.name}"/>
                <apex:column value="{!acc.rating}"/>
                <apex:column value="{!acc.industry}"/>
        </apex:pageBlockTable>
        </apex:pageBlock>
    </apex:form>
</apex:page>